Auto Fetch kubectl config

Usage:
  otc-login -h | --help
  otc-login -v | --version
  otc-login [options]
Options:
  -h --help                                 Show this screen.
  -v --version                              Show version.
  --projectName <projectId>                	OTC Project Name. If omitted, the OS_PROJECT_NAME environment variable are used.
  --cluster <cluster>                		Name of the Kubernetes Cluster
  --token <token>                         	Authentication Token for OTC
  --ak <ak>                            		Access Key for OTC Authentication. If omitted, the OS_ACCESS_KEY environment variable is used.
  --sk <sk>                            		Secret Key for OTC Authentication. If omitted, the OS_SECRET_KEY environment variable is used.